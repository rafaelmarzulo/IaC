galaxy_info:
  author: "Gemini AI for @rafael"
  description: "Role para configurar backups automatizados via rsync para um servidor remoto."
  company: "N/A"
  license: "MIT"

  min_ansible_version: "2.12"

  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all

dependencies:
  - role: core/baseline
