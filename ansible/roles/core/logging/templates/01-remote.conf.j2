# Configuração para encaminhamento de logs para servidor remoto
# Gerenciado por Ansible

# Encaminha todas as mensagens de todos os níveis
*.* @{{ '@@' if logging_remote_protocol == 'tcp' else '@' }}{{ logging_remote_host }}:{{ logging_remote_port }}
